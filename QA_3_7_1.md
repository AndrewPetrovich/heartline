# HEARTLINE 3.7.1 QA

This hotfix addresses two release-engineering defects in the 3.7 migration:

1. CRLF/LF-sensitive multiline matching.
2. Unix-only `find | xargs` in `npm run check`.

The business architecture target from 3.7 is unchanged:
Presentation → Application → Domain/Ports ← Infrastructure.

Local hotfix validation:
- corrected migration script: node --check PASS
- cross-platform syntax checker: node --check PASS
- syntax checker self-run on this package: PASS

Full application regression tests must be run in the complete repository after
the migration is successfully applied.
