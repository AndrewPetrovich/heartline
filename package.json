{
  "name": "heartline-editor",
  "version": "3.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check heartline-db.js && node --check heartline-assets.js && node --check sw.js && find hl-editor -name '*.js' -print0 | xargs -0 -n1 node --check && node tools/verify-font-policy.mjs",
    "verify-repository": "node tools/verify-repository.mjs"
  }
}
