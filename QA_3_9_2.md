# HEARTLINE 3.9.2 QA

Regression contracts:

- no-image Final Review renders the device synchronously;
- the device frame always receives `unit.text` and `unit.speaker`;
- async asset loading is not required for text visualization;
- the synchronous shell is rendered before event binding;
- compact `NICO · text · Редактировать` remains visible while editing;
- the full editor stacks below the compact row;
- editorial styles explicitly keep rendered player devices visible;
- 3.9.1 async/stale-render/error-state protections remain intact.

Local combined regression suite:
13 tests / 13 PASS.

New 3.9.2 tests:
6 tests / 6 PASS.

JavaScript syntax:
PASS.
