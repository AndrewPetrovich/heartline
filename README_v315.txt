HEARTLINE Editor 3.1.5 — Graph Connections Repair

Исправлен режим «Граф → Маршруты»:
- отображаются реальные направленные связи между показанными нодами;
- показываются все Choice внутри главы, а не только первый;
- варианты A/B/C разводятся отдельными кривыми и больше не накладываются в одну линию;
- промежуточные линейные сцены сворачиваются в переходы;
- точки слияния/расхождения учитываются при выборе ключевых нод;
- у branch-lattice сценариев без постоянных route lanes убран вводящий в заблуждение серый широкий поток;
- широкие цветные route flows остаются только у историй, где реальные долгоживущие маршруты присутствуют.

Загрузите все файлы в корень репозитория. После deployment один раз откройте /upgrade-v315.html. IndexedDB не очищается.
