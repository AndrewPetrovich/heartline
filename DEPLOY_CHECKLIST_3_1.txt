HEARTLINE 3.1 deployment

Recommended: replace repository contents with the FULL package using GitHub Desktop.

1. Keep the repository itself and its .git folder.
2. Delete old project files from the local clone.
3. Copy every file/folder from the FULL package into the repository root.
4. Commit: "HEARTLINE 3.1 quality pass".
5. Push origin.
6. Wait for GitHub Pages deployment.
7. Open /upgrade-v310.html once.
8. Then use the normal /heartline/ address.

Do not clear IndexedDB manually. Existing local projects/versions/reviews remain in the browser.
