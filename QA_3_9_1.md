# HEARTLINE 3.9.1 QA

Regression contracts:

- no-image frame is passed to the player renderer with `assetUrl = null`;
- Preview render is delayed until layout paint;
- stale asynchronous Preview renders are discarded;
- blank Preview becomes an inline error state with Retry;
- final text editor is rendered in the center Preview workspace;
- final text is flushed before fragment/stage navigation;
- final editor uses debounce autosave;
- internal navigation away from the workspace starts persistence of a pending
  final edit;
- toast does not overlap the bottom Preview caption;
- no new DB/Infrastructure dependency is introduced in Presentation.

Local package tests: 7/7 PASS.
New JavaScript syntax: PASS.

Run the full repository suite after migration.
